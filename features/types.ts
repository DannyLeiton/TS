/* Important to note: 
JS Types are: Primitives or Objects.
Primitives: number, boolean, string, symbol, null, void.
Objects: function, array, class, object.
*/
//typeof symbol;
const today = new Date();
today.getMonth();

const person = {
  age: 20
};
class Color {}
const red = new Color();
